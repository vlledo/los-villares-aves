# Bitácora Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Añadir una bitácora pública al sitio que liste novedades del catálogo (aves nuevas + mejoras) en formato micropost, con página propia `/bitacora/`, bloques en portada y entrada en la navegación.

**Architecture:** Nueva colección Astro `bitacora` con frontmatter mínimo (`fecha`, `especies?`) y cuerpo MDX libre. Un fichero utilidad concentra el procesado (formato de fechas, agrupación por mes, resolución de slugs de `especies` contra el catálogo `aves` con validación cruzada en build). Página única `/bitacora/` y dos bloques nuevos en `index.astro`. Sin tests unitarios formales —el proyecto no tiene framework— la validación se hace con `npm run build` (Zod valida el frontmatter) y revisión visual.

**Tech Stack:** Astro 6, MDX, Tailwind 4, Zod (vía Astro Content Collections).

**Spec:** [`../specs/2026-06-12-bitacora-design.md`](../specs/2026-06-12-bitacora-design.md)

---

## Task 1: Añadir colección `bitacora` al schema

**Files:**
- Modify: `src/content.config.ts`

- [ ] **Step 1: Añadir la nueva colección al schema**

Editar `src/content.config.ts` para añadir, después de la definición de `aves` y antes del `export`:

```typescript
const bitacora = defineCollection({
	loader: glob({ base: './src/content/bitacora', pattern: '**/*.{md,mdx}' }),
	schema: z.object({
		fecha: z.date(),
		especies: z.array(z.string()).min(1).optional(),
	}),
});

export const collections = { aves, bitacora };
```

Sustituir la línea actual `export const collections = { aves };` por la nueva.

- [ ] **Step 2: Verificar que el build sigue pasando sin entradas todavía**

Run: `npm run build`
Expected: build OK, 106 páginas. Astro no romperá porque la colección está vacía.

- [ ] **Step 3: Commit**

```bash
git add src/content.config.ts
git commit -m "Añadir colección 'bitacora' al schema de contenido"
```

---

## Task 2: Crear las tres entradas iniciales (seed)

**Files:**
- Create: `src/content/bitacora/2026-06-07-catalogo-101-especies.mdx`
- Create: `src/content/bitacora/2026-06-12-categoria-raro.mdx`
- Create: `src/content/bitacora/2026-06-12-repaso-completo.mdx`

- [ ] **Step 1: Crear la entrada de la ampliación a 101**

Crear `src/content/bitacora/2026-06-07-catalogo-101-especies.mdx`:

```markdown
---
fecha: 2026-06-07
---

El catálogo llega a 101 especies, más de la mitad de las registradas
en eBird a diez kilómetros del pueblo. Quedan los rapaces más
esquivos y algunos paseriformes del pinar.
```

- [ ] **Step 2: Crear la entrada de la categoría raro**

Crear `src/content/bitacora/2026-06-12-categoria-raro.mdx`:

```markdown
---
fecha: 2026-06-12
---

Estrenamos la categoría "raro" en el calendario, en color rosa. Se
reserva para especies que aparecen de forma irregular: el
[jilguero lúgano](/aves/jilguero-lugano/) en años de irrupción, el
[águila imperial](/aves/aguila-imperial-iberica/) de paso, el
[buitre negro](/aves/buitre-negro/) en recolonización lenta.
```

- [ ] **Step 3: Crear la entrada del repaso completo**

Crear `src/content/bitacora/2026-06-12-repaso-completo.mdx`:

```markdown
---
fecha: 2026-06-12
---

Hoy queda cerrado el repaso del catálogo: las 101 fichas tienen ya
todas sus secciones completas —comportamiento, canto, dónde verlas,
identificación, calendario y conservación. Quedan más uniformes y se
leen mucho mejor.
```

- [ ] **Step 4: Verificar build con las 3 entradas**

Run: `npm run build`
Expected: build OK, sin errores de Zod. Si Astro avisa de errores de fecha, comprobar que el YAML usa `fecha: 2026-06-12` sin comillas (Zod la convierte a `Date`).

- [ ] **Step 5: Commit**

```bash
git add src/content/bitacora/
git commit -m "Añadir entradas iniciales de la bitácora (repaso, categoría raro, hito 101)"
```

---

## Task 3: Utilidad para procesar entradas de la bitácora

**Files:**
- Create: `src/utils/bitacora.ts`

Este módulo concentra el procesado de la colección: formato de fechas, agrupación por mes, resolución de slugs de `especies` contra el catálogo. Vive aparte de las páginas para que ambas (`/bitacora/` y `/index`) lo usen sin duplicar lógica.

- [ ] **Step 1: Crear el archivo con las funciones de utilidad**

Crear `src/utils/bitacora.ts`:

```typescript
import { type CollectionEntry, getCollection } from 'astro:content';

const MESES_CORTOS = [
	'ene', 'feb', 'mar', 'abr', 'may', 'jun',
	'jul', 'ago', 'sep', 'oct', 'nov', 'dic',
];

const MESES_LARGOS = [
	'Enero', 'Febrero', 'Marzo', 'Abril', 'Mayo', 'Junio',
	'Julio', 'Agosto', 'Septiembre', 'Octubre', 'Noviembre', 'Diciembre',
];

export function formatoFechaCorta(d: Date): string {
	return `${d.getDate()} ${MESES_CORTOS[d.getMonth()]} ${d.getFullYear()}`;
}

export function formatoMesLargo(d: Date): string {
	return `${MESES_LARGOS[d.getMonth()]} ${d.getFullYear()}`;
}

export type EntradaBitacora = CollectionEntry<'bitacora'> & {
	especiesResueltas: CollectionEntry<'aves'>[];
};

/**
 * Devuelve todas las entradas de la bitácora ordenadas por fecha
 * descendente, con los slugs de `especies` resueltos a entradas del
 * catálogo. Si una entrada referencia un slug que no existe, lanza un
 * error en build con el path del .mdx y el slug problemático.
 */
export async function obtenerEntradasBitacora(): Promise<EntradaBitacora[]> {
	const [entradas, aves] = await Promise.all([
		getCollection('bitacora'),
		getCollection('aves'),
	]);
	const avesPorSlug = new Map(aves.map((a) => [a.id, a]));

	return entradas
		.map((entrada) => {
			const especiesResueltas = (entrada.data.especies ?? []).map((slug) => {
				const ave = avesPorSlug.get(slug);
				if (!ave) {
					throw new Error(
						`bitacora/${entrada.id}.mdx: especie "${slug}" no existe en el catálogo`,
					);
				}
				return ave;
			});
			return { ...entrada, especiesResueltas };
		})
		.sort((a, b) => {
			const dt = b.data.fecha.getTime() - a.data.fecha.getTime();
			return dt !== 0 ? dt : a.id.localeCompare(b.id);
		});
}

/**
 * Agrupa las entradas (ya ordenadas) por "Mes Año" en español.
 * Devuelve array de pares [titulo, entradas] preservando el orden.
 */
export function agruparPorMes(
	entradas: EntradaBitacora[],
): Array<[string, EntradaBitacora[]]> {
	const grupos = new Map<string, EntradaBitacora[]>();
	for (const e of entradas) {
		const clave = formatoMesLargo(e.data.fecha);
		const lista = grupos.get(clave) ?? [];
		lista.push(e);
		grupos.set(clave, lista);
	}
	return Array.from(grupos.entries());
}

/**
 * Devuelve los `n` slugs únicos más recientes encontrados en
 * entradas con `especies` (en orden cronológico inverso). Útil para
 * el bloque "Últimas añadidas" de portada.
 */
export function ultimasEspeciesUnicas(
	entradas: EntradaBitacora[],
	n: number,
): CollectionEntry<'aves'>[] {
	const vistos = new Set<string>();
	const resultado: CollectionEntry<'aves'>[] = [];
	for (const entrada of entradas) {
		for (const ave of entrada.especiesResueltas) {
			if (vistos.has(ave.id)) continue;
			vistos.add(ave.id);
			resultado.push(ave);
			if (resultado.length >= n) return resultado;
		}
	}
	return resultado;
}
```

- [ ] **Step 2: Verificar que tipa correctamente (sin uso real aún)**

Run: `npx astro check 2>&1 | tail -10`
Expected: 0 errors, 0 warnings (puede que dé un error si la utilidad no se usa todavía en ningún sitio; eso es aceptable solo si el error es "unused"; cualquier error de tipo real hay que arreglarlo).

Nota: si `astro check` no está disponible, ejecutar `npm run build` que también valida tipos en MDX y fallaría si la sintaxis es errónea.

- [ ] **Step 3: Commit**

```bash
git add src/utils/bitacora.ts
git commit -m "Añadir utilidad para procesar entradas de la bitácora"
```

---

## Task 4: Página `/bitacora/`

**Files:**
- Create: `src/pages/bitacora.astro`

- [ ] **Step 1: Crear la página completa**

Crear `src/pages/bitacora.astro`:

```astro
---
import { Image } from 'astro:assets';
import { render } from 'astro:content';
import Layout from '../layouts/Layout.astro';
import {
	agruparPorMes,
	formatoFechaCorta,
	obtenerEntradasBitacora,
} from '../utils/bitacora';

const entradas = await obtenerEntradasBitacora();
const grupos = agruparPorMes(entradas);

const entradasRender = await Promise.all(
	entradas.map(async (e) => {
		const { Content } = await render(e);
		return [e.id, Content] as const;
	}),
);
const contentPorId = new Map(entradasRender);
---

<Layout title="Bitácora" description="Novedades del catálogo de aves de Los Villares: nuevas especies, mejoras y cambios.">
	<article class="mx-auto max-w-3xl">
		<header class="border-b border-tierra-200 pb-6">
			<h1 class="text-4xl font-bold tracking-tight text-tierra-900">
				Bitácora
			</h1>
			<p class="mt-3 text-lg text-tierra-700">
				Novedades del catálogo: nuevas aves añadidas, mejoras de las fichas y
				cambios del sitio. Notas breves en orden cronológico.
			</p>
		</header>

		{
			grupos.length === 0 && (
				<p class="mt-8 text-tierra-600">
					Aún no hay entradas en la bitácora.
				</p>
			)
		}

		{
			grupos.map(([titulo, entradasMes]) => (
				<section class="mt-10">
					<h2 class="text-sm font-semibold uppercase tracking-wide text-tierra-700">
						{titulo}
					</h2>
					<div class="mt-4 space-y-8">
						{entradasMes.map((entrada) => {
							const Content = contentPorId.get(entrada.id)!;
							return (
								<article class="border-l-2 border-olivo-200 pl-4">
									<div class="text-xs uppercase tracking-wide text-tierra-500">
										{formatoFechaCorta(entrada.data.fecha)}
									</div>
									<div class="prose prose-stone mt-2 max-w-none text-tierra-800">
										<Content />
									</div>
									{entrada.especiesResueltas.length > 0 && (
										<div class="mt-3 flex gap-3 overflow-x-auto pb-1 [scrollbar-width:thin]">
											{entrada.especiesResueltas.map((ave) => (
												<a
													href={`/aves/${ave.id}/`}
													class="group block w-28 shrink-0 no-underline"
												>
													{ave.data.foto_principal && (
														<Image
															src={ave.data.foto_principal}
															alt={`Foto de ${ave.data.nombre_comun}`}
															width={240}
															height={160}
															class="aspect-[3/2] w-full rounded-md object-cover transition-transform group-hover:scale-[1.03]"
														/>
													)}
													<div class="mt-1 text-xs font-medium text-tierra-800 group-hover:text-olivo-700">
														{ave.data.nombre_comun}
													</div>
												</a>
											))}
										</div>
									)}
								</article>
							);
						})}
					</div>
				</section>
			))
		}
	</article>
</Layout>
```

- [ ] **Step 2: Verificar build con la nueva página**

Run: `npm run build`
Expected: build OK, 107 páginas (106 anteriores + bitacora). Sin errores.

- [ ] **Step 3: Verificar visualmente**

Run: `npm run dev`
Abrir `http://localhost:4321/bitacora/`. Comprobar:
- Las 3 entradas aparecen agrupadas bajo "Junio 2026".
- Fechas: "12 jun 2026" (dos veces), "7 jun 2026".
- Orden: las dos del 12 jun primero, luego la del 7 jun.
- Los enlaces a especies en la entrada de "categoría raro" llevan a sus fichas.
- No hay franja de miniaturas (ninguna entrada inicial tiene `especies`).

Detener `npm run dev` con Ctrl+C.

- [ ] **Step 4: Commit**

```bash
git add src/pages/bitacora.astro
git commit -m "Añadir página /bitacora/ con vista cronológica"
```

---

## Task 5: Añadir "Bitácora" al Header

**Files:**
- Modify: `src/components/Header.astro:4-9`

- [ ] **Step 1: Insertar la entrada en la navegación**

En `src/components/Header.astro`, sustituir el array `links` actual por:

```typescript
const links = [
	{ href: '/', label: 'Inicio' },
	{ href: '/aves', label: 'Aves' },
	{ href: '/calendario', label: 'Calendario' },
	{ href: '/bitacora', label: 'Bitácora' },
	{ href: '/sobre', label: 'Sobre' },
];
```

- [ ] **Step 2: Verificar build y visualización**

Run: `npm run build`
Expected: build OK.

Run: `npm run dev`
Comprobar:
- En cualquier página, el header muestra "Bitácora" como cuarta entrada.
- Al navegar a `/bitacora/`, esa entrada queda resaltada (estilo `aria-current`).

Detener con Ctrl+C.

- [ ] **Step 3: Commit**

```bash
git add src/components/Header.astro
git commit -m "Añadir entrada 'Bitácora' al menú de navegación"
```

---

## Task 6: Bloque "Bitácora" en portada

**Files:**
- Modify: `src/pages/index.astro`

Se añade primero este bloque (más sencillo) antes del de "Últimas añadidas" porque ahora mismo no hay entradas con `especies` y "Últimas añadidas" quedaría vacío hasta el primer lote de importación.

- [ ] **Step 1: Modificar `index.astro` para añadir el bloque "Bitácora"**

Editar `src/pages/index.astro`. Primero, modificar el import existente de `astro:content` para incluir `render`, y añadir el import de la utilidad de bitácora.

Sustituir:
```typescript
import { getCollection } from 'astro:content';
```
por:
```typescript
import { getCollection, render } from 'astro:content';
import { formatoFechaCorta, obtenerEntradasBitacora } from '../utils/bitacora';
```

Después de `const destacadas = ...` añadir, antes del cierre del frontmatter `---`:

```typescript
const entradasBitacora = await obtenerEntradasBitacora();
const ultimosMicropost = entradasBitacora.slice(0, 3);
const ultimosMicropostRender = await Promise.all(
	ultimosMicropost.map(async (e) => {
		const { Content } = await render(e);
		return [e.id, Content] as const;
	}),
);
const contentMicropost = new Map(ultimosMicropostRender);
```

Luego, en el cuerpo del componente, añadir el bloque "Bitácora" justo después del bloque `{ destacadas.length > 0 && (...) }` y antes de `<section class="mt-10 grid gap-6 sm:grid-cols-3">` (las stats):

```astro
{
	ultimosMicropost.length > 0 && (
		<section class="mt-10">
			<div class="mb-4 flex items-baseline justify-between">
				<h2 class="text-sm font-semibold uppercase tracking-wide text-tierra-700">
					Bitácora
				</h2>
				<a
					href="/bitacora"
					class="text-sm text-olivo-700 no-underline hover:underline"
				>
					Ver toda la bitácora →
				</a>
			</div>
			<div class="space-y-6">
				{ultimosMicropost.map((entrada) => {
					const Content = contentMicropost.get(entrada.id)!;
					return (
						<article class="border-l-2 border-olivo-200 pl-4">
							<div class="text-xs uppercase tracking-wide text-tierra-500">
								{formatoFechaCorta(entrada.data.fecha)}
							</div>
							<div class="prose prose-stone mt-2 max-w-none text-tierra-800">
								<Content />
							</div>
						</article>
					);
				})}
			</div>
		</section>
	)
}
```

Nota: este bloque deliberadamente NO renderiza `especiesResueltas` —cuando haya entradas con especies, sus miniaturas viven en el bloque "Últimas añadidas" (Task 7) justo encima.

- [ ] **Step 2: Verificar build y visualización**

Run: `npm run build`
Expected: build OK.

Run: `npm run dev`
Abrir `http://localhost:4321/`. Comprobar:
- Aparece la sección "Bitácora" entre el carrusel de destacadas y las stats.
- Muestra las 3 entradas (las dos del 12 jun + la del 7 jun).
- Enlace "Ver toda la bitácora →" funciona.

Detener con Ctrl+C.

- [ ] **Step 3: Commit**

```bash
git add src/pages/index.astro
git commit -m "Añadir bloque 'Bitácora' a la portada"
```

---

## Task 7: Bloque "Últimas añadidas" en portada

**Files:**
- Modify: `src/pages/index.astro`

- [ ] **Step 1: Ampliar imports e incluir cálculo de últimas especies**

En `src/pages/index.astro`, ampliar el import de `bitacora` para incluir `ultimasEspeciesUnicas`:

```typescript
import {
	formatoFechaCorta,
	obtenerEntradasBitacora,
	ultimasEspeciesUnicas,
} from '../utils/bitacora';
```

Después de la definición de `entradasBitacora`, añadir:

```typescript
const ultimasAves = ultimasEspeciesUnicas(entradasBitacora, 4);
```

- [ ] **Step 2: Añadir el bloque "Últimas añadidas" antes del de "Bitácora", al final del Layout**

Nota: el bloque "Bitácora" se reubicó al final del Layout (debajo del CTA "Ver el catálogo") por feedback del usuario. "Últimas añadidas" debe ir **justo antes** del bloque "Bitácora" pero también después del CTA — pasa a ser el primer bloque tras el CTA. Usar `mt-12` (para marcar la separación con el CTA) y cambiar el `mt-12` actual del bloque "Bitácora" a `mt-10` (ya no es el primero del grupo de novedades).

En el cuerpo del componente, **antes** del bloque `{ ultimosMicropost.length > 0 && (...) }` añadido en Task 6, insertar:

```astro
{
	ultimasAves.length > 0 && (
		<section class="mt-12">
			<div class="mb-4 flex items-baseline justify-between">
				<h2 class="text-sm font-semibold uppercase tracking-wide text-tierra-700">
					Últimas añadidas
				</h2>
				<a
					href="/bitacora"
					class="text-sm text-olivo-700 no-underline hover:underline"
				>
					Ver todas las novedades →
				</a>
			</div>
			<div class="grid grid-cols-2 gap-3 sm:grid-cols-4">
				{ultimasAves.map((ave) => (
					<a
						href={`/aves/${ave.id}/`}
						class="group block overflow-hidden rounded-lg border border-tierra-200 bg-white no-underline shadow-sm"
					>
						{ave.data.foto_principal && (
							<Image
								src={ave.data.foto_principal}
								alt={`Foto de ${ave.data.nombre_comun}`}
								width={400}
								height={260}
								class="aspect-[3/2] w-full object-cover transition-transform duration-300 group-hover:scale-[1.03]"
							/>
						)}
						<div class="px-3 py-2">
							<div class="text-sm font-semibold text-tierra-900 group-hover:text-olivo-700">
								{ave.data.nombre_comun}
							</div>
							<div class="text-xs italic text-tierra-500">
								{ave.data.nombre_cientifico}
							</div>
						</div>
					</a>
				))}
			</div>
		</section>
	)
}
```

Nota: el componente `Image` **no está** todavía importado en `index.astro` (el carrusel lo importa en su propio archivo). Añadir al bloque de imports del frontmatter:

```typescript
import { Image } from 'astro:assets';
```

- [ ] **Step 3: Verificar build**

Run: `npm run build`
Expected: build OK.

- [ ] **Step 4: Verificar visualmente**

Run: `npm run dev`
Abrir `http://localhost:4321/`. Comprobar:
- Como ninguna entrada inicial tiene `especies`, el bloque "Últimas añadidas" **no aparece** (la guarda `ultimasAves.length > 0` lo oculta).
- El bloque "Bitácora" sigue apareciendo correctamente.

Para probar el bloque manualmente, añadir temporalmente `especies: [abejaruco-europeo, abubilla, mirlo-comun, urraca-comun]` a la entrada `2026-06-12-categoria-raro.mdx`, rebuilear, refrescar el navegador y comprobar:
- Aparece "Últimas añadidas" con esas 4 fotos.
- Cada foto enlaza a su ficha.

Quitar las `especies` después de la prueba (no son reales para esa entrada).

Detener `npm run dev` con Ctrl+C.

- [ ] **Step 5: Commit**

```bash
git add src/pages/index.astro
git commit -m "Añadir bloque 'Últimas añadidas' a la portada"
```

---

## Task 8: Documentar en CLAUDE.md y memoria

**Files:**
- Modify: `CLAUDE.md`
- Modify: `~/.claude/projects/-Users-vlledo-Documents-workspace-aves-villares/memory/MEMORY.md`
- Create: `~/.claude/projects/-Users-vlledo-Documents-workspace-aves-villares/memory/reference_bitacora.md`

- [ ] **Step 1: Añadir sección sobre la bitácora en CLAUDE.md**

En `CLAUDE.md`, después de la sección "Estado del catálogo y próximo lote" y antes de "Flujo para añadir o sincronizar especies", insertar:

```markdown
## Bitácora

Colección `bitacora` (definida en `src/content.config.ts`) con entradas en `src/content/bitacora/*.mdx`. Cada entrada es un **micropost**: fecha + 1-2 frases sobre una novedad del sitio. Tono cercano y breve, sin título.

**Frontmatter:**
```yaml
fecha: 2026-06-12              # obligatoria, ISO
especies:                      # opcional, solo si la entrada anuncia aves nuevas
  - lavandera-cascadena
```

- `fecha` es obligatoria; Zod la convierte a `Date`.
- `especies` es opcional; si está presente debe tener al menos un slug y todos deben existir en la colección `aves` (validado en build por `src/utils/bitacora.ts → obtenerEntradasBitacora`, que rompe con error claro si encuentra un slug inválido).
- Cuerpo MDX libre. Para enlazar especies: `[nombre común](/aves/<slug>/)`.

**Convención de nombre:** `YYYY-MM-DD-slug-breve.mdx`.

**Cuándo crear entrada:**
- Al importar nuevas especies → entrada con `especies: [...]` listando los slugs.
- Al hacer una mejora transversal (criterio nuevo, secciones nuevas, hitos como llegar a 150 fichas) → entrada sin `especies`.

**Dónde se ve:**
- Página `/bitacora/` lista todas las entradas agrupadas por mes.
- Portada (`src/pages/index.astro`) muestra dos bloques: "Últimas añadidas" (4 fotos de las últimas especies aparecidas en entradas con `especies`) y "Bitácora" (3 últimos micropost de cualquier tipo).
- Entrada "Bitácora" en `src/components/Header.astro`.

**Spec original:** [`docs/superpowers/specs/2026-06-12-bitacora-design.md`](docs/superpowers/specs/2026-06-12-bitacora-design.md).
```

- [ ] **Step 2: Crear memoria persistente sobre la bitácora**

Crear `~/.claude/projects/-Users-vlledo-Documents-workspace-aves-villares/memory/reference_bitacora.md`:

```markdown
---
name: reference-bitacora
description: Colección bitácora del sitio — cuándo crear entrada y formato del micropost
metadata:
  type: reference
---

El sitio tiene una colección `bitacora` en `src/content/bitacora/*.mdx` con micropost de novedades (aves nuevas y mejoras). La definición completa vive en `CLAUDE.md` (sección "Bitácora") y el spec original en `docs/superpowers/specs/2026-06-12-bitacora-design.md`.

**Cuándo crear entrada (siempre que importes algo o hagas un cambio relevante):**
- Importas nuevas especies → entrada con `especies: [slug1, slug2, ...]`.
- Mejora transversal (criterio nuevo en alguna sección, hito, cambio de schema) → entrada sin `especies`.

**Forma del micropost:**
- Sin título.
- 1-2 frases por entrada.
- Tono cercano y breve (no blog corto, no changelog técnico).
- Cuando menciones especies, enlázalas: `[lavandera cascadeña](/aves/lavandera-cascadena/)`.
- Nombre de archivo: `YYYY-MM-DD-slug-breve.mdx`.

**Why:** El usuario quiere una bitácora pública para visitantes recurrentes, manual y con voz propia. Validado en sesión 2026-06-12. Decisión clave: NO se auto-deriva de `fecha_anadida` en las fichas (manual a propósito).

**How to apply:** Después de cada importación o mejora visible al usuario, crear una entrada nueva en `src/content/bitacora/` antes de cerrar la sesión. Verificar con `npm run build` (rompe si un slug en `especies` no existe).
```

- [ ] **Step 3: Añadir el puntero al índice de memoria**

En `~/.claude/projects/-Users-vlledo-Documents-workspace-aves-villares/memory/MEMORY.md`, añadir una línea al final:

```markdown
- [Bitácora](reference_bitacora.md) — colección de micropost en `src/content/bitacora/`; crear entrada tras cada importación o mejora
```

- [ ] **Step 4: Verificar build final**

Run: `npm run build`
Expected: build OK, 107 páginas.

- [ ] **Step 5: Commit**

```bash
git add CLAUDE.md docs/superpowers/
git commit -m "Documentar la bitácora en CLAUDE.md y archivar spec + plan"
```

Nota: el spec en `docs/superpowers/specs/2026-06-12-bitacora-design.md` y el plan en `docs/superpowers/plans/2026-06-12-bitacora.md` también entran en este commit porque hasta ahora estaban sin trackear.

---

## Verificación final

Después del último commit:

- [ ] **Step 1: Build limpio**

Run: `npm run build 2>&1 | tail -5`
Expected: build OK, **107 páginas** (101 fichas + index + aves + calendario + sobre + bitacora + 404).

- [ ] **Step 2: Smoke test en navegador**

Run: `npm run dev`

Recorrer:
- `/` — verifica orden: hero → carrusel destacadas → Bitácora (3 entradas) → stats → CTA. "Últimas añadidas" no aparece (correcto, sin entradas con `especies`).
- `/bitacora/` — verifica que las 3 entradas aparecen agrupadas bajo "Junio 2026", con fechas correctas.
- Cualquier ficha de `/aves/...` — verifica que el header muestra "Bitácora" como cuarta entrada.
- Los enlaces internos a especies dentro del micropost de "categoría raro" funcionan.

Detener con Ctrl+C.

- [ ] **Step 3: Validar `**` no se han colado en frontmatter de bitácora**

Run:
```bash
for f in src/content/bitacora/*.mdx; do
  awk '/^---$/{fm=!fm; next} fm && /\*\*/{c++} END{print c+0}' "$f"
done | awk '{s+=$1} END{print s}'
```
Expected: `0`.
